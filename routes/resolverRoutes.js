import userResolver from "../resolvers/userResolver.js";
import gameRsolver from "../resolvers/gameResolver.js";

const resolverlist = [
    userResolver,
    gameRsolver
]

export default resolverlist